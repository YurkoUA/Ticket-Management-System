@model PartialViewModel

@{
    ViewBag.Title = Model.Title;
    bool isAdmin = User.IsInRole("Admin");
}

<div id="serial-grid">
    @{ 
        Html.RenderPartial(Model.PartialViewName, Model.ModelObject);
    }
</div>

@if (isAdmin)
{
    <div id="serial-result"></div>

    { 
        Html.RenderPartial("ToolbarPartial", Model.ModelId);
    }
}
<br />

<div>
    @Html.ActionLink("На до списку", "Index")
</div>

@section scripts {
    @Scripts.Render("~/Scripts/packages-modal.js", "~/Scripts/serial.js")
}