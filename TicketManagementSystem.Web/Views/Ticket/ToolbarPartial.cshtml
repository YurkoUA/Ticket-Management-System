@model int

@{
    Layout = null;
    var ajaxOptions = new AjaxOptions { UpdateTargetId = "ticket-grid", OnBegin = "OnBegin" };
    var routeValues = new { id = Model };
}

<div class="dropdown">
    <button class="btn btn-default dropdown-toggle" type="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
        Керувати
        <span class="caret"></span>
    </button>

    <ul class="dropdown-menu">
        <li>@Ajax.ActionLink("Деталі", "Details", routeValues, ajaxOptions, new { })</li>
        <li>@Ajax.ActionLink("Редагувати", "Edit", routeValues, ajaxOptions, new { })</li>
        <li>@Ajax.ActionLink("Видалити", "Delete", routeValues, ajaxOptions, new { })</li>

        <li role="separator" class="divider"></li>

        <li>@Ajax.ActionLink("Змінити номер", "ChangeNumber", routeValues, ajaxOptions, new { })</li>
        <li>@Ajax.ActionLink("Перемістити", "Move", routeValues, ajaxOptions, new { })</li>
    </ul>
</div>
