@model TicketIndexModel

@{
    ViewBag.Title = "Нерозподілені квитки";
}


@if (Model.Any())
{
    <h2>
        @ViewBag.Title
        <small>@Model.PageInfo.TotalItems шт.</small>
    </h2>

    <div class="panel panel-danger">
        @*<div class="panel-heading">Нерозподілених квитків: @Model.PageInfo.TotalItems</div>*@
        <div class="panel-heading">Сторінка @Model.PageInfo.PageNumber з @Model.PageInfo.TotalPages</div>

        <div class="table-responsive">
            <table class="table table-hover table-bordered">
                <tr>
                    <th>Номер</th>
                    <th>Колір</th>
                    <th>Серія</th>
                    <th>Дата</th>
                    <th>Примітка</th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr class="@item.TrClass()">
                        <td>@Html.ActionLink(item.Number, "Details", new { id = item.Id })</td>
                        <td><a href="@Url.Action("Details", "Color", new { id = item.ColorId })">@item.ColorName</a></td>
                        <td><a href="@Url.Action("Details", "Serial", new { id = item.SerialId })">@item.SerialFull</a></td>
                        <td>@item.Date</td>
                        <td>@item.Note</td>
                        <td>@Html.ActionLink("Перемістити до пачки", "Move", new { id = item.Id })</td>
                    </tr>
                }
            </table>
        </div>

        @if (Model.PageInfo.TotalPages > 1)
        {
            <div class="panel-footer">
                @Html.PageLinks(Model.PageInfo, x => Url.Action("Unallocated", new
                {
                    page = x
                }))
            </div>
        }
    </div>
}
else
{
    <h2>@ViewBag.Title</h2>
    @Html.AlertDanger("Нерозподілених квитків немає.")
}

<div>
    @Html.ActionLink("Назад до списку", "Index")
</div>