@using TicketManagementSystem.Enumerations;

@model PartialType

@{
    
}

<div id="colorGrid">
    @switch (Model)
    {
        case PartialType.Details:
            @Html.Partial("DetailsPartial", ViewBag.ViewModel as ColorDetailsModel);
            break;

        case PartialType.Edit:
            @Html.Partial("EditPartial", ViewBag.ViewModel as ColorEditModel);
            break;

        case PartialType.Delete:
            @Html.Partial("DeletePartial", ViewBag.ViewModel as ColorDetailsModel);
            break;
    }
</div>

<div id="colorResult">

</div>